# Copy this file to .env and fill in your values.
# .env is gitignored — never commit your real credentials.

PLOP_BASE_URL=https://api.example.com

# Add one variable per secret header value:
PLOP_AUTH_TOKEN=your-token-here
PLOP_BYPASS_TOKEN=your-bypass-token-here
